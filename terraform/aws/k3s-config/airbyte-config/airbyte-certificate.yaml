apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: airbyte-tls
  namespace: airbyte
spec:
  secretName: airbyte-tls
  dnsNames:
    - ${airbyte_hostname}
  issuerRef:
    name: local-issuer
    kind: ClusterIssuer
    group: cert-manager.io